<div class="col-lg-12 mt-5">
  <%= link_to "Back to Dashboard", admin_dashboard_path, class: "btn btn-primary shadow" %>
  <%= link_to "View All Bookings", admin_bookings_path, class: "btn btn-primary shadow" %>
  <div class="row mt-5">
    <div class="col-lg-7">
      <div class="card shadow">
        <div class="card-body">
          <h4 class="card-title"><%= @workshop.name %></h4>
          <p  class="lead"><%= @workshop.description %></p>
          <h4><span class="badge bg-primary"><%= @workshop.total_duration%></span></h4>
          <h4><span class="badge bg-primary"><%= @workshop.daily_duration %></span></h4>
          <p class="lead"> Total Sits: <span class="badge bg-primary"><%= @workshop.total_sits %></span></p>
          <p class="lead"> Remaining Sits: <span class="badge bg-primary"><%= @workshop.remaining_sits %></span></p>
          <p class="lead"> Registration Fee: <span class="badge bg-primary"><%= @workshop.registration_fee %></span></p>
        </div>
      </div>
    </div>
    <div class="col-lg-5">
      <div class="card shadow">
        <div class="card-body">
          <h4 class="card-title">Customer Details</h4>
          <hr>
          <p class="lead"><strong>Name: </strong><%= @customer.full_name %></p>
          <p class="lead"><strong>Email: </strong><%= @customer.email %></p>
          <p class="lead"><strong>Contact No.</strong><%= @customer.contact_number %></p>
        </div>
      </div>
    </div>
  </div>
</div>